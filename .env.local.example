# Copy to .env.local (gitignored, never committed) to enable a small in-browser
# role picker that bypasses GitHub login entirely — for local testing only.
# See README "Local development" for what it does and why it's safe.
VITE_LOCAL_DEV_LOGIN=true
