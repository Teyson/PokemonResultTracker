# Node
node_modules/
npm-debug.log*

# SvelteKit
.svelte-kit/
build/

# Azure Functions local
api/local.settings.json
api/dist/
bin/
obj/
.python_packages/

# Drizzle CLI env (SQL_* creds for db:generate/db:migrate — separate from local.settings.json)
# and the frontend's local-only dev-login flag (VITE_LOCAL_DEV_LOGIN) — never committed,
# so a real deploy never has a way to enable either.
.env
.env.local
.env.*.local
api/.env

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
