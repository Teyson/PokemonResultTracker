# Node
node_modules/
npm-debug.log*

# SvelteKit
.svelte-kit/
build/

# Azure Functions local
api/local.settings.json
api/dist/
bin/
obj/
.python_packages/

# Drizzle CLI env (SQL_* creds for db:generate/db:migrate — separate from local.settings.json),
# never committed so a real deploy never has a way to leak them.
.env
.env.local
.env.*.local
api/.env

# Claude Code local settings (machine-specific permissions — launch.json IS committed)
.claude/settings.local.json

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
