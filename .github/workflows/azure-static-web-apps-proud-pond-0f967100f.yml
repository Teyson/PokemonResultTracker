name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main

jobs:
  # Applies any pending Drizzle migrations to the production database before
  # the new API code is deployed. drizzle-kit migrate tracks already-applied
  # migrations itself, so this is a no-op push with no new migration files.
  migrate_database:
    runs-on: ubuntu-latest
    name: Migrate Database
    permissions:
      id-token: write # required for the OIDC federated login below
      contents: read
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      # Azure SQL's firewall rejects everything by default. GitHub-hosted
      # runners don't have a stable egress IP — even within a single job,
      # consecutive connections can go out through different addresses from
      # Azure's outbound NAT pool — and GitHub's published Actions IP ranges
      # (5000+ CIDR blocks across Azure/AWS/GCP) are too numerous for
      # per-block firewall rules and too broad to be a meaningful allow-list
      # anyway. So instead this opens the firewall to all IPs for just the
      # few seconds this job needs it, then closes it again — the exposure
      # window is bounded by time (one CI run), not by IP.
      - name: Azure login (OIDC, no stored secret)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Open temporary SQL firewall rule for this run
        run: |
          az sql server firewall-rule create \
            --resource-group PokemonResultTracker \
            --server pokemonresulttracker \
            --name gh-actions-ci-temp \
            --start-ip-address 0.0.0.0 \
            --end-ip-address 255.255.255.255
          # Firewall changes can take a few seconds to propagate.
          sleep 15
      - run: npm run db:migrate
        env:
          SQL_SERVER: ${{ secrets.SQL_SERVER }}
          SQL_DATABASE: ${{ secrets.SQL_DATABASE }}
          SQL_USER: ${{ secrets.SQL_USER }}
          SQL_PASSWORD: ${{ secrets.SQL_PASSWORD }}
      - name: Close temporary SQL firewall rule
        if: always()
        continue-on-error: true
        run: |
          az sql server firewall-rule delete \
            --resource-group PokemonResultTracker \
            --server pokemonresulttracker \
            --name gh-actions-ci-temp

  build_and_deploy_job:
    needs: [migrate_database]
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PROUD_POND_0F967100F }}
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "api" # Api source code path - optional
          output_location: "build" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######
